# This is an example file for the Docker Compose setup.
# Copy this to a .env file at the root of the project.

# --- Backend Environment Variables ---
# These are passed to the backend service.
DEBUG=True
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"
JWT_SECRET_KEY="a_very_secret_key_for_docker"

# --- Frontend Environment Variables ---
# These are passed to the frontend service.
# Note: The frontend uses NEXT_PUBLIC_API_BASE_URL to know where the backend is.
# Inside the Docker network, the backend is reachable by its service name 'backend'.
NEXT_PUBLIC_API_BASE_URL=http://backend:8000

# --- Database Credentials ---
# These are used to initialize the MongoDB service.
MONGO_USER=mongoadmin
MONGO_PASS=secretpassword
