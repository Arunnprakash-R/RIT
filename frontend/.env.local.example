# This file is an example. Copy it to .env.local for development.

# The URL of the backend API. This is used by the frontend to make requests.
# When running with Docker Compose, the backend will be available at this address
# from the frontend container's perspective.
NEXT_PUBLIC_API_BASE_URL=http://backend:8000
