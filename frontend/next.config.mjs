/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration options for Next.js can be added here.
  // For example, to set up rewrites or redirects.
};

export default nextConfig;
